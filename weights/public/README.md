Download or place the released SGAT public weights here.

Expected filenames:

- `sgat_matcher_public.pth` (`sgat.*` only)
- `superpoint_extractor_public.pth`

`COP` weights are stored separately in `../cop/last.ckpt`.
